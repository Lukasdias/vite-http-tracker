{
  "name": "vite-http-tracker",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "type": "module",
  "scripts": {
    "test": "bun test",
    "dev:server": "bun run packages/server/src/cli.ts",
    "dev:all:react": "concurrently -k -n tracker,react -c blue,green \"bun run packages/server/src/cli.ts --no-open\" \"bun run --cwd apps/react-app dev\"",
    "dev:all:vue": "concurrently -k -n tracker,vue -c blue,green \"bun run packages/server/src/cli.ts --no-open\" \"bun run --cwd apps/vue-app dev\"",
    "dev:all:solid": "concurrently -k -n tracker,solid -c blue,green \"bun run packages/server/src/cli.ts --no-open\" \"bun run --cwd apps/solid-app dev\"",
    "dev:all:svelte": "concurrently -k -n tracker,svelte -c blue,green \"bun run packages/server/src/cli.ts --no-open\" \"bun run --cwd apps/svelte-app dev\"",
    "dev:all:preact": "concurrently -k -n tracker,preact -c blue,green \"bun run packages/server/src/cli.ts --no-open\" \"bun run --cwd apps/preact-app dev\"",
    "dev:all:vanilla": "concurrently -k -n tracker,vanilla -c blue,green \"bun run packages/server/src/cli.ts --no-open\" \"bun run --cwd apps/vanilla-app dev\"",
    "build": "bun run build --filter=*",
    "build:ui": "bun run --cwd packages/ui build",
    "lint": "oxlint packages apps",
    "format": "oxfmt --write packages apps"
  },
  "devDependencies": {
    "@happy-dom/global-registrator": "^15.0.0",
    "@types/bun": "latest",
    "concurrently": "^10.0.5",
    "happy-dom": "^15.0.0",
    "oxfmt": "^0.66.0",
    "oxlint": "^1.81.0",
    "typescript": "^5.5.0"
  }
}
